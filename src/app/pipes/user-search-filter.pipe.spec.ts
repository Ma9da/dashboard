import { UserSearchFielterPipe } from './user-search-filter.pipe';

describe('UserSearchFielterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserSearchFielterPipe();
    expect(pipe).toBeTruthy();
  });
});
